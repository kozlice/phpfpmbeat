nginx:
  build: ./etc/nginx
  ports:
    - "80:80"
  links:
    - fpm

fpm:
  build: ./etc/phpfpm
  ports:
    - "9000:9000"

elasticsearch:
  image: elasticsearch:1.7.3
  ports:
    - "9200:9200"

kibana:
  image: kibana:4.1
  ports:
    - "5601:5601"
  links:
    - elasticsearch
